<?php
/**
 * MODELO. Copie este arquivo para  config.php  (na mesma pasta, /aplicacao/)
 * direto no servidor da KingHost e preencha a chave.
 *
 * NAO comite config.php (ja esta no .gitignore). A chave fica so no servidor.
 * A MESMA chave precisa ser validada no Control, no header X-API-Key.
 */

define('CONTROL_URL', 'https://aptec-control.replit.app/api/webhooks/leads');
define('CONTROL_API_KEY', 'COLE_AQUI_A_CHAVE');
